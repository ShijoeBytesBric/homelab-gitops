# Never commit the age private key or local kubeconfig
age.agekey
*.key
kubeconfig
*.tfstate

# Local Claude Code settings (permissions allow-list, tool prefs) — machine-local
.claude/settings.local.json

# Ansible inventory is regenerated by deploy.sh on every run
01-provisioning/ansible/inventory/
